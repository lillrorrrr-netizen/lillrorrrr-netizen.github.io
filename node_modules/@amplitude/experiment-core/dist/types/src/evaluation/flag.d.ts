export type EvaluationFlag = {
    key: string;
    variants: Record<string, EvaluationVariant>;
    segments: EvaluationSegment[];
    dependencies?: string[];
    metadata?: Record<string, unknown>;
};
export type EvaluationVariant = {
    key?: string;
    value?: unknown;
    payload?: unknown;
    metadata?: Record<string, unknown>;
};
export type EvaluationSegment = {
    bucket?: EvaluationBucket;
    conditions?: EvaluationCondition[][];
    variant?: string;
    metadata?: Record<string, unknown>;
};
export type EvaluationBucket = {
    selector: string[];
    salt: string;
    allocations: EvaluationAllocation[];
};
export type EvaluationCondition = {
    selector: string[];
    op: string;
    values: string[];
};
export type EvaluationAllocation = {
    range: number[];
    distributions: EvaluationDistribution[];
};
export type EvaluationDistribution = {
    variant: string;
    range: number[];
};
/** @alpha */
export type EvaluationConditionResult = {
    propValue: unknown;
    condition: EvaluationCondition;
    matched: boolean;
};
/** @alpha */
export type EvaluationSegmentResult = {
    segmentMetadata?: Record<string, unknown>;
    conditionsPassed: boolean;
    bucketed: boolean;
    bucketVariant?: string;
    conditionResult?: (EvaluationConditionResult[] | undefined)[];
    matched: boolean;
};
/** @alpha */
export type FlagEvaluationTrace = {
    flagKey: string;
    matched: boolean;
    matchedSegment?: string;
    steps: EvaluationSegmentResult[];
};
export declare const EvaluationOperator: {
    readonly IS: "is";
    readonly IS_NOT: "is not";
    readonly CONTAINS: "contains";
    readonly DOES_NOT_CONTAIN: "does not contain";
    readonly LESS_THAN: "less";
    readonly LESS_THAN_EQUALS: "less or equal";
    readonly GREATER_THAN: "greater";
    readonly GREATER_THAN_EQUALS: "greater or equal";
    readonly VERSION_LESS_THAN: "version less";
    readonly VERSION_LESS_THAN_EQUALS: "version less or equal";
    readonly VERSION_GREATER_THAN: "version greater";
    readonly VERSION_GREATER_THAN_EQUALS: "version greater or equal";
    readonly SET_IS: "set is";
    readonly SET_IS_NOT: "set is not";
    readonly SET_CONTAINS: "set contains";
    readonly SET_DOES_NOT_CONTAIN: "set does not contain";
    readonly SET_CONTAINS_ANY: "set contains any";
    readonly SET_DOES_NOT_CONTAIN_ANY: "set does not contain any";
    readonly REGEX_MATCH: "regex match";
    readonly REGEX_DOES_NOT_MATCH: "regex does not match";
};
